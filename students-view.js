/* Dingel Hafizia — Google Sheet style Students module: 3 views */
import { getCollection, addDocument, updateDocument, deleteDocument, setDocument } from "./firestore.js";

const view=document.getElementById("view");
const MONTHS=["APRL","MAY","JUN","JUL","AUG","SEP","OCT","NOV","DEC","JAN","FEB","MAR"];
const CLASS_OPTIONS=["HIFZ","MAKTAB","ADNA ALIF"];
let students=[];let activeTab="master";let feeRows=[];let feeAcademicYear=academicYear();
function academicYear(){const d=new Date();const y=d.getMonth()>=3?d.getFullYear():d.getFullYear()-1;return `${y}-${String(y+1).slice(-2)}`}
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const money=v=>new Intl.NumberFormat("en-IN",{maximumFractionDigits:2}).format(Number(v)||0);
const initials=n=>String(n||"S").trim().split(/\s+/).slice(0,2).map(x=>x[0]).join("").toUpperCase();
const nameOf=s=>s.name||s.studentName||"Unnamed";const rollOf=s=>s.roll??s.rol??s.rollNo??s.studentId??"—";const fatherOf=s=>s.fatherName||s.father||s.guardian||"—";const phoneOf=s=>s.phone||s.number||s.phoneNumber||"—";const feeRaw=s=>s.monthly??s.monthlyFee??s.feeCategory??s.fee??"-";
function feeAmount(s){const v=String(feeRaw(s)).trim().toUpperCase();if(!v||v==="-"||v==="ATIM"||v==="N RSD"||v==="N_RSD")return 0;const n=Number(String(feeRaw(s)).replace(/[^0-9.]/g,""));return Number.isFinite(n)?n:0}
function feeLabel(s){const raw=String(feeRaw(s)).trim();const n=feeAmount(s);return n>0?`₹${money(n)}`:(raw.toUpperCase()==="ATIM"?"ATIM":raw.toUpperCase()==="N RSD"||raw.toUpperCase()==="N_RSD"?"N RSD":"-")}
const isActive=s=>String(s.status||"Active").toLowerCase()!=="inactive";const isOrphan=s=>String(feeRaw(s)).trim().toUpperCase()==="ATIM"||s.studentType==="Orphan";const totalTarget=list=>list.reduce((a,s)=>a+feeAmount(s),0);
function toast(msg,error=false){const r=document.getElementById("toastRoot");if(!r)return;r.textContent=msg;r.className=error?"toast error":"toast";clearTimeout(window.__toast);window.__toast=setTimeout(()=>{r.textContent="";r.className=""},3000)}
async function loadStudents(){try{students=await getCollection("students")}catch(e){students=[];console.error(e)}window.__dhStudents=students}
function tabBar(){return `<div class="sheet-tabs"><button class="sheet-tab ${activeTab==="master"?"active":""}" data-sheet-tab="master">1. Master Student List</button><button class="sheet-tab ${activeTab==="active"?"active":""}" data-sheet-tab="active">2. Active Students List</button><button class="sheet-tab ${activeTab==="monthly"?"active":""}" data-sheet-tab="monthly">3. Monthly Collection & Due</button></div>`}
function header(){return `<div class="page-header"><div><h3>Students Details</h3><p>Google Sheet-এর ৩টি page অনুযায়ী শিক্ষার্থী ও ফি ব্যবস্থাপনা</p></div><div class="actions"><button class="btn primary" id="addStudentBtn">＋ Add Student</button></div></div>${tabBar()}`}
function masterPage(){const search=(document.getElementById("sheetSearch")?.value||"").toLowerCase().trim();const rows=students.filter(s=>!search||[rollOf(s),nameOf(s),fatherOf(s),s.address||"",s.class||"",phoneOf(s),feeRaw(s)].join(" ").toLowerCase().includes(search));const target=totalTarget(students);view.innerHTML=`<div class="content sheet-page">${header()}<div class="sheet-summary"><div><small>Total Registered Students</small><b>${students.length}</b></div><div><small>Total Potential Monthly Fees</small><b>₹${money(target)}</b></div><div><small>Active</small><b>${students.filter(isActive).length}</b></div><div><small>Inactive / Left</small><b>${students.filter(s=>!isActive(s)).length}</b></div></div><div class="sheet-toolbar"><div class="student-search"><span>⌕</span><input id="sheetSearch" type="search" placeholder="Search name, roll, father, address or phone" value="${esc(search)}"></div><button class="btn outline" id="masterRefresh">↻ Refresh</button></div><div class="card sheet-card"><div class="card-header"><h3>DINGEL HAFIZIA MADRASAH STUDENTS DETAILS</h3><span>${rows.length} of ${students.length}</span></div><div class="sheet-scroll"><table class="sheet-table"><thead><tr><th>SL</th><th>ROL</th><th>Student Name</th><th>Father's Name</th><th>ADDRESS</th><th>Class</th><th>Number</th><th>MONTHLY</th><th>Status</th><th>Options</th></tr></thead><tbody>${rows.length?rows.map((s,i)=>masterRow(s,i)).join(""):emptyRow(10)}</tbody></table></div></div></div>`;bindCommon();document.getElementById("sheetSearch")?.addEventListener("input",masterPage);document.getElementById("masterRefresh")?.addEventListener("click",async()=>{await loadStudents();masterPage()})}
function masterRow(s,i){return `<tr><td>${i+1}</td><td><b>${esc(rollOf(s))}</b></td><td><div class="sheet-person"><span>${esc(initials(nameOf(s)))}</span><b>${esc(nameOf(s))}</b></div></td><td>${esc(fatherOf(s))}</td><td>${esc(s.address||"—")}</td><td><span class="sheet-badge">${esc(s.class||"—")}</span></td><td>${esc(phoneOf(s))}</td><td><b>${esc(feeLabel(s))}</b></td><td>${isActive(s)?'<span class="status-badge active">Active</span>':'<span class="status-badge inactive">Inactive</span>'}</td><td><div class="sheet-actions"><button data-action="view" data-id="${esc(s.id)}">👁</button><button data-action="edit" data-id="${esc(s.id)}">✎</button><button data-action="delete" data-id="${esc(s.id)}">🗑</button></div></td></tr>`}
function activePage(){const search=(document.getElementById("sheetSearch")?.value||"").toLowerCase().trim();const all=students.filter(isActive);const rows=all.filter(s=>!search||[rollOf(s),nameOf(s),fatherOf(s),s.address||"",s.class||"",phoneOf(s),feeRaw(s)].join(" ").toLowerCase().includes(search));const target=totalTarget(all);view.innerHTML=`<div class="content sheet-page">${header()}<div class="sheet-summary"><div><small>Total Active Students</small><b>${all.length}</b></div><div><small>Total Active Monthly Fee Target</small><b>₹${money(target)}</b></div><div><small>ATIM / Free</small><b>${all.filter(isOrphan).length}</b></div><div><small>Classes</small><b>${new Set(all.map(s=>s.class).filter(Boolean)).size}</b></div></div><div class="sheet-toolbar"><div class="student-search"><span>⌕</span><input id="sheetSearch" type="search" placeholder="Search active student, roll, guardian or phone" value="${esc(search)}"></div><button class="btn outline" id="activeRefresh">↻ Refresh</button></div><div class="card sheet-card"><div class="card-header"><h3>PRESENT STUDENTS DETAILS</h3><span>${rows.length} of ${all.length}</span></div><div class="sheet-scroll"><table class="sheet-table"><thead><tr><th>S.L</th><th>ROL</th><th>STUDENTS</th><th>GUARDIAN</th><th>ADDRESS</th><th>CLASS</th><th>PH NO</th><th>FEES</th><th>Options</th></tr></thead><tbody>${rows.length?rows.map((s,i)=>activeRow(s,i)).join(""):emptyRow(9)}</tbody></table></div></div></div>`;bindCommon();document.getElementById("sheetSearch")?.addEventListener("input",activePage);document.getElementById("activeRefresh")?.addEventListener("click",async()=>{await loadStudents();activePage()})}
function activeRow(s,i){return `<tr><td>${i+1}</td><td><b>${esc(rollOf(s))}</b></td><td><div class="sheet-person"><span>${esc(initials(nameOf(s)))}</span><b>${esc(nameOf(s))}</b></div></td><td>${esc(fatherOf(s))}</td><td>${esc(s.address||"—")}</td><td><span class="sheet-badge">${esc(s.class||"—")}</span></td><td>${esc(phoneOf(s))}</td><td><b>${esc(feeLabel(s))}</b>${isOrphan(s)?'<small class="free-note">Free</small>':''}</td><td><div class="sheet-actions"><button data-action="view" data-id="${esc(s.id)}">👁</button><button data-action="edit" data-id="${esc(s.id)}">✎</button><button data-action="delete" data-id="${esc(s.id)}">🗑</button></div></td></tr>`}
function emptyRow(n){return `<tr><td colspan="${n}"><div class="student-empty"><div class="empty-icon">👨‍🎓</div><h4>No student records</h4><p>Add students to populate this sheet.</p><button class="btn primary small" id="emptyAddBtn">＋ Add Student</button></div></td></tr>`}
async function loadFees(){try{feeRows=await getCollection("monthlyPayments")}catch(e){feeRows=[];console.error(e)}}
function paymentFor(student,month){return feeRows.find(x=>x.studentId===student.id&&x.month===month&&x.academicYear===feeAcademicYear)||null}
function paymentText(s,m){const p=paymentFor(s,m);if(!p||Number(p.paidAmount||0)<=0)return "—";const paid=Number(p.paidAmount||0),base=feeAmount(s);if(base<=0)return "✓ paid";if(paid>=base)return "✓ paid";return `₹${money(paid)}`}
function paymentClass(s,m){const p=paymentFor(s,m);if(!p||Number(p.paidAmount||0)<=0)return "unpaid";const paid=Number(p.paidAmount||0),base=feeAmount(s);return base>0&&paid<base?"partial":"paid"}
async function monthlyPage(){await loadFees();const search=(document.getElementById("sheetSearch")?.value||"").toLowerCase().trim();const rows=students.filter(isActive).filter(s=>!search||[rollOf(s),nameOf(s),phoneOf(s)].join(" ").toLowerCase().includes(search));const monthSummary=MONTHS.map(m=>{let collected=0,due=0,target=0;rows.forEach(s=>{const base=feeAmount(s);target+=base;const p=paymentFor(s,m);const paid=Math.min(base,Number(p?.paidAmount||0));collected+=paid;due+=Math.max(0,base-paid)});return {m,collected,due,target}});view.innerHTML=`<div class="content sheet-page monthly-sheet-page">${header()}<div class="monthly-toolbar"><div><label>Academic Year</label><select id="feeYear"><option>${feeAcademicYear}</option></select></div><div class="student-search"><span>⌕</span><input id="sheetSearch" type="search" placeholder="Search active student, roll or phone" value="${esc(search)}"></div><button class="btn outline" id="feeRefresh">↻ Refresh</button></div><div class="fee-legend"><span><i class="paid-dot"></i> ✓ Paid</span><span><i class="partial-dot"></i> Partial</span><span><i class="unpaid-dot"></i> Unpaid / Due</span><span>Click any month cell to collect</span></div><div class="card sheet-card"><div class="card-header"><h3>MONTHLY COLLECTION & DUE TRACKER</h3><span>${rows.length} Active Students</span></div><div class="sheet-scroll fee-matrix-scroll"><table class="sheet-table fee-matrix"><thead><tr><th>ROL</th><th>STUDENTS</th><th>PH NO</th><th>FEES</th>${MONTHS.map(m=>`<th>${m}</th>`).join("")}</tr></thead><tbody>${rows.length?rows.map(s=>feeMatrixRow(s)).join(""):emptyRow(4+MONTHS.length)}</tbody><tfoot><tr class="summary-collected"><th colspan="4">Total Collected (জমা হয়েছে)</th>${monthSummary.map(x=>`<td>₹${money(x.collected)}</td>`).join("")}</tr><tr class="summary-due"><th colspan="4">Total Outstanding / Due (বাকি আছে)</th>${monthSummary.map(x=>`<td>₹${money(x.due)}</td>`).join("")}</tr><tr class="summary-target"><th colspan="4">Target Total (সর্ব মোট)</th>${monthSummary.map(x=>`<td>₹${money(x.target)}</td>`).join("")}</tr></tfoot></table></div></div></div>`;document.getElementById("sheetSearch")?.addEventListener("input",monthlyPage);document.getElementById("feeRefresh")?.addEventListener("click",monthlyPage);document.querySelectorAll("[data-fee-cell]").forEach(b=>b.onclick=()=>openCollectFee(b.dataset.student,b.dataset.month));bindTabsOnly()}
function feeMatrixRow(s){return `<tr><td><b>${esc(rollOf(s))}</b></td><td><div class="sheet-person"><span>${esc(initials(nameOf(s)))}</span><b>${esc(nameOf(s))}</b></div></td><td>${esc(phoneOf(s))}</td><td><b>${esc(feeLabel(s))}</b></td>${MONTHS.map(m=>`<td><button class="fee-cell ${paymentClass(s,m)}" data-fee-cell="1" data-student="${esc(s.id)}" data-month="${m}">${paymentText(s,m)}</button></td>`).join("")}</tr>`}
async function openCollectFee(studentId,month){const s=students.find(x=>x.id===studentId);if(!s)return;const p=paymentFor(s,month);const base=feeAmount(s),paid=Number(p?.paidAmount||0),due=Math.max(0,base-paid);modal(`<div class="card-header"><div><h3>Collect Fee — ${esc(month)}</h3><p class="text-muted">${esc(nameOf(s))} · Roll ${esc(rollOf(s))}</p></div><button class="icon-btn" id="closeCollect">×</button></div><form id="collectForm"><div class="card-body"><div class="collect-summary"><div><small>Monthly Fee</small><b>${feeLabel(s)}</b></div><div><small>Already Paid</small><b>₹${money(paid)}</b></div><div><small>Current Due</small><b>₹${money(due)}</b></div></div><div class="form-group"><label>Payment Amount (₹)</label><input id="collectAmount" name="amount" type="number" min="0" step="0.01" value="${base>0?due:0}" ${base<=0?"disabled":""} required></div><div id="collectPreview" class="collect-preview"></div><div class="form-group"><label>Note</label><input name="note" value="${esc(p?.note||"")}" placeholder="Optional note"></div><div class="form-footer"><button type="button" class="btn outline" id="cancelCollect">Cancel</button><button type="submit" class="btn primary">Save Payment</button></div></div></form>`);const amountEl=document.getElementById("collectAmount"),preview=document.getElementById("collectPreview");const updatePreview=()=>{const add=Number(amountEl?.value||0),total=paid+add;if(base<=0){preview.innerHTML='<span class="status-badge active">FREE / NO COLLECTION</span>';return}preview.innerHTML=total>=base?'<span class="status-badge active">✓ PAID — No Due</span>':`<span class="status-badge partial">PARTIAL — Due ₹${money(base-total)}</span>`};amountEl?.addEventListener("input",updatePreview);updatePreview();document.getElementById("closeCollect").onclick=closeModal;document.getElementById("cancelCollect").onclick=closeModal;document.getElementById("collectForm").onsubmit=async e=>{e.preventDefault();const add=Number(amountEl?.value||0);const total=paid+add;if(add<0||(base>0&&total>base)){toast("Payment cannot exceed monthly fee.",true);return}try{const btn=e.target.querySelector('[type="submit"]');btn.disabled=true;btn.textContent="Saving…";const data={studentId:s.id,studentName:nameOf(s),roll:rollOf(s),month,academicYear:feeAcademicYear,baseFee:base,paidAmount:total,dueAmount:Math.max(0,base-total),status:base<=0?"FREE":total>=base?"PAID":total>0?"PARTIAL":"UNPAID",note:e.target.note.value.trim(),updatedAt:new Date().toISOString()};if(p)await setDocument("monthlyPayments",p.id,data);else await addDocument("monthlyPayments",data);closeModal();toast(`${month} payment saved.`);await monthlyPage()}catch(err){console.error(err);toast("Could not save payment. Check Firestore permissions.",true)}}}
function modal(content){document.getElementById("modalRoot").innerHTML=`<div class="modal-backdrop" id="dhModal"><div class="modal student-modal">${content}</div></div>`;document.getElementById("dhModal").onclick=e=>{if(e.target.id==="dhModal")closeModal()}}
function closeModal(){document.getElementById("modalRoot").innerHTML=""}
function inputField(label,name,value="",type="text",required=false){return `<div class="form-group"><label>${label}${required?' <span class="required-star">*</span>':''}</label><input name="${name}" type="${type}" value="${esc(value)}" ${required?"required":""}></div>`}
function openStudentForm(id){const s=id?students.find(x=>x.id===id):{};const fee=feeRaw(s);modal(`<div class="card-header"><div><h3>${id?"Edit Student":"Add New Student"}</h3><p class="text-muted">Master Student List fields</p></div><button class="icon-btn" id="closeStudent">×</button></div><form id="studentForm"><div class="card-body"><div class="student-form-grid">${inputField("ROL / Roll Number","roll",s.roll??s.rol??s.rollNo||"")}${inputField("Student Name","name",nameOf(s)==="Unnamed"?"":nameOf(s),"text",true)}${inputField("Father's Name","fatherName",s.fatherName||s.father||s.guardian||"")}${inputField("Number / Phone","phone",phoneOf(s)==="—"?"":phoneOf(s),"tel")}<div class="form-group"><label>Class <span class="required-star">*</span></label><select name="class" required>${CLASS_OPTIONS.map(c=>`<option value="${c}" ${s.class===c?"selected":""}>${c}</option>`).join("")}</select></div><div class="form-group"><label>MONTHLY</label><select name="monthly"><option value="-" ${String(fee)==="-"?"selected":""}>-</option><option value="500" ${String(fee)==="500"?"selected":""}>500</option><option value="700" ${String(fee)==="700"?"selected":""}>700</option><option value="1000" ${String(fee)==="1000"?"selected":""}>1000</option><option value="ATIM" ${String(fee).toUpperCase()==="ATIM"?"selected":""}>ATIM</option><option value="N RSD" ${String(fee).toUpperCase()==="N RSD"||String(fee).toUpperCase()==="N_RSD"?"selected":""}>N RSD</option></select></div><div class="form-group full-width"><label>ADDRESS</label><input name="address" value="${esc(s.address||"")}"></div><div class="form-group"><label>Status</label><select name="status"><option value="Active" ${isActive(s)?"selected":""}>Active</option><option value="Inactive" ${!isActive(s)?"selected":""}>Inactive / Left</option></select></div></div><div class="form-footer"><button type="button" class="btn outline" id="cancelStudent">Cancel</button><button type="submit" class="btn primary">${id?"Save Changes":"Add Student"}</button></div></div></form>`);document.getElementById("closeStudent").onclick=closeModal;document.getElementById("cancelStudent").onclick=closeModal;document.getElementById("studentForm").onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));data.roll=data.roll.trim();data.rol=data.roll;data.name=data.name.trim();data.fatherName=data.fatherName.trim();data.guardian=data.fatherName;data.phone=data.phone.trim();data.number=data.phone;data.address=data.address.trim();data.class=data.class.trim();data.status=data.status;data.monthly=data.monthly;data.studentType=data.monthly==="ATIM"?"Orphan":"General";try{const b=e.target.querySelector('[type="submit"]');b.disabled=true;b.textContent="Saving…";if(id)await updateDocument("students",id,data);else await addDocument("students",data);closeModal();toast(id?"Student updated successfully.":"Student added successfully.");await loadStudents();activeTab==="master"?masterPage():activeTab==="active"?activePage():monthlyPage()}catch(err){console.error(err);toast("Could not save student. Check Firestore permissions.",true)}}}
function viewStudent(id){const s=students.find(x=>x.id===id);if(!s)return;modal(`<div class="card-header"><div><h3>Student Details</h3><p class="text-muted">${esc(nameOf(s))} · Roll ${esc(rollOf(s))}</p></div><button class="icon-btn" id="closeStudent">×</button></div><div class="card-body"><div class="student-detail-head"><div class="student-detail-avatar">${esc(initials(nameOf(s)))}</div><div><h4>${esc(nameOf(s))}</h4><p>${esc(s.class||"—")} · ${esc(feeLabel(s))}</p></div></div><div class="student-detail-grid">${[["ROL",rollOf(s)],["Student Name",nameOf(s)],["Father's Name",fatherOf(s)],["ADDRESS",s.address||"—"],["Class",s.class||"—"],["Number",phoneOf(s)],["MONTHLY",feeLabel(s)],["Status",isActive(s)?"Active":"Inactive / Left"]].map(([a,b])=>`<div class="student-detail-item"><small>${a}</small><strong>${esc(b)}</strong></div>`).join("")}</div><div class="form-footer"><button class="btn outline" id="closeProfile">Close</button><button class="btn primary" id="editProfile">Edit Student</button></div></div>`);document.getElementById("closeStudent").onclick=closeModal;document.getElementById("closeProfile").onclick=closeModal;document.getElementById("editProfile").onclick=()=>{closeModal();openStudentForm(id)}}
async function deleteStudent(id){const s=students.find(x=>x.id===id);if(!s)return;if(!confirm(`Delete student “${nameOf(s)}”? This cannot be undone.`))return;try{await deleteDocument("students",id);toast("Student deleted successfully.");await loadStudents();activeTab==="master"?masterPage():activePage()}catch(e){console.error(e);toast("Could not delete student. Check Firestore permissions.",true)}}
function bindCommon(){bindTabsOnly();document.querySelectorAll('[data-action="view"]').forEach(b=>b.onclick=()=>viewStudent(b.dataset.id));document.querySelectorAll('[data-action="edit"]').forEach(b=>b.onclick=()=>openStudentForm(b.dataset.id));document.querySelectorAll('[data-action="delete"]').forEach(b=>b.onclick=()=>deleteStudent(b.dataset.id));document.getElementById("addStudentBtn")?.addEventListener("click",()=>openStudentForm());document.getElementById("emptyAddBtn")?.addEventListener("click",()=>openStudentForm())}
function bindTabsOnly(){document.querySelectorAll("[data-sheet-tab]").forEach(b=>b.onclick=async()=>{activeTab=b.dataset.sheetTab;await loadStudents();renderStudents()})}
function renderStudents(){if(activeTab==="master")masterPage();else if(activeTab==="active")activePage();else monthlyPage()}
function interceptNav(){const n=document.getElementById("nav");if(!n)return;const b=n.querySelector('[data-page="students"]');if(b&&b.dataset.sheetBound!=="1"){b.dataset.sheetBound="1";b.onclick=async()=>{activeTab="master";await loadStudents();renderStudents()}}}
const observer=new MutationObserver(interceptNav);if(document.getElementById("nav"))observer.observe(document.getElementById("nav"),{childList:true,subtree:true});window.addEventListener("dh:auth",async()=>{interceptNav();if(document.querySelector('[data-page="students"].active')){await loadStudents();renderStudents()}});setTimeout(interceptNav,500);window.__dhReloadStudents=async()=>{await loadStudents();renderStudents()};