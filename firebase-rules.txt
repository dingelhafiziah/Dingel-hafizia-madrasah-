rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    /* DINGEL HAFIZIA MADRASA ERP */

    match /madrasa/{document} { allow read, write: if true; }
    match /students/{studentId} { allow read, write: if true; }
    match /fees/{feeId} { allow read, write: if true; }
    match /income/{incomeId} { allow read, write: if true; }
    match /expenses/{expenseId} { allow read, write: if true; }
    match /accounts/{accountId} { allow read, write: if true; }
    match /reports/{reportId} { allow read, write: if true; }
    match /settings/{settingId} { allow read, write: if true; }
    match /activity/{activityId} { allow read, write: if true; }

    // Normalized Fees Management v2 collections.
    // These explicit matches keep the data model documented and ready for
    // production role-based rules when the project moves out of testing mode.
    match /feeStructures/{structureId} { allow read, write: if true; }
    match /feeTransactions/{transactionId} { allow read, write: if true; }
    match /monthlyPayments/{paymentId} { allow read, write: if true; }

    match /{document=**} { allow read, write: if true; }
  }
}
